extends layout

block content
  div(class="content")
    h2 Stats
    ul(style="padding: 0")
      each applicant in applicants
        div
          span #{applicant.name} (#{applicant.candidacyScore})
